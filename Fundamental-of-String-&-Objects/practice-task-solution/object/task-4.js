let student = {
      name: 'Ariana Grande',
      age: 21,
      city: 'Gaibandha',
      isStudent: true
};

// with Object.keys().length method
let size = Object.keys(student).length;
console.log(size);

// with Object.values().length method
let length = Object.values(student).length;
console.log(length);

